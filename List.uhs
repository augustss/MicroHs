module List where

-- By parser some parser hacks we can use [] instead of Nil
data List a = Nil | (:) a (List a)

map :: (a -> b) -> [a] -> [b]
map f arg =
  case arg of
    [] -> []
    (:) a as -> f a : map f as
